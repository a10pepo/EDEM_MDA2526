services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: salvagisbert/entregable_docker
    working_dir: /app
    volumes:
      - ".:/app"
    entrypoint: ["python", "main.py"]
