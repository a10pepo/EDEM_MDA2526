# Conexión a PostgreSQL (RDS) y Redshift (protocolo PostgreSQL)
psycopg2-binary==2.9.9

# SDK de AWS (S3, Glue, aprovisionamiento)
boto3>=1.34

# Carga de variables de entorno desde .env
python-dotenv>=1.0

# Data lakehouse: Apache Iceberg con catálogo Glue.
# El acceso a S3 lo gestiona PyArrow (PyArrowFileIO), por eso NO usamos el
# extra [s3fs]: evita el conflicto aiobotocore/botocore que bloquea pip.
pyiceberg[glue]>=0.7.0
pyarrow>=15.0
