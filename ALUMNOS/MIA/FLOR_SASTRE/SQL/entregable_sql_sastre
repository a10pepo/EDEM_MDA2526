-- -- -- -- --CREATE DATABASE employees;

-- -- -- CREATE TABLE IF NOT EXISTS employees (
-- -- --   id              BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY NOT NULL,
-- -- --   first_name      VARCHAR(100) NOT NULL,
-- -- --   last_name       VARCHAR(100) NOT NULL,
-- -- --   birth_date       DATE NOT NULL            
-- -- -- );

-- -- -- --ALTER TABLE employees ADD COLUMN salary NUMERIC(10,2);
-- -- -- ALTER TABLE employees ADD COLUMN title VARCHAR(100);
-- -- -- ALTER TABLE employees ADD COLUMN title_date DATE;
-- -- -- ALTER TABLE employees ADD COLUMN department_id 

-- -- -- INSERT INTO employees (first_name, last_name, birth_date, salary, title, title_date)
-- -- -- VALUES
-- -- -- -- 👥 3 con el mismo nombre (Lionel)
-- -- -- ('Lionel', 'Messi', '1987-06-24', 50000.00, 'CEO', '2022-01-01'),
-- -- -- ('Lionel', 'Suarez', '1990-04-10', 28000.00, 'Team Lead', '2020-05-15'),
-- -- -- ('Lionel', 'Paredes', '1988-03-12', 22000.00, 'Analyst', '2020-07-20'),

-- -- -- -- 💼 Otros empleados
-- -- -- ('Maria', 'Lopez', '1992-09-15', 15000.00, 'Marketing Manager', '2020-03-10'),
-- -- -- ('Sofia', 'Martinez', '1995-07-09', 18000.00, 'UX Designer', '2021-08-05'),
-- -- -- ('Lucas', 'Fernandez', '1991-11-22', 12000.00, 'Sales Executive', '2020-09-30'),
-- -- -- ('Pedro', 'Garcia', '1986-02-05', 9000.00, 'HR Specialist', '2022-02-10'),
-- -- -- ('Martina', 'Perez', '1998-06-17', 8000.00, 'Intern', '2023-01-15'),
-- -- -- ('Carla', 'Gomez', '1993-04-12', 17000.00, 'Content Strategist', '2021-11-20'),
-- -- -- ('Juan', 'Torres', '1989-05-08', 25000.00, 'Software Engineer', '2020-01-22'),
-- -- -- ('Valentina', 'Rossi', '1997-03-14', 13000.00, 'Community Manager', '2023-04-10'),
-- -- -- ('Andres', 'Diaz', '1985-10-25', 10000.00, 'QA Tester', '2019-06-01'),
-- -- -- ('Camila', 'Ortega', '1994-12-19', 45000.00, 'CTO', '2022-09-10'),
-- -- -- ('Federico', 'Sanchez', '1990-08-30', 16000.00, 'Data Analyst', '2020-02-18'),
-- -- -- ('Lucia', 'Ramirez', '1987-01-11', 27000.00, 'Project Manager', '2020-11-25');


-- -- -- SELECT * FROM employees;
-- -- -- SELECT first_name, salary FROM employees;

-- -- -- SELECT * FROM employees
-- -- -- WHERE id = 2;

-- -- -- SELECT * FROM employees
-- -- -- WHERE salary > 20000;

-- -- -- SELECT * FROM employees
-- -- -- WHERE salary <= 10000; 

-- -- -- UPDATE employees SET first_name = 'Moral' WHERE id = 7;
-- -- -- DELETE FROM employees WHERE salary > 20000;

-- -- -- SELECT * FROM employees; 

-- -- -- SELECT * FROM employees WHERE salary BETWEEN 14000 AND 50000;
-- -- -- SELECT * FROM employees ORDER BY birth_date DESC;

-- -- -- SELECT DISTINT first_name FROM employees;
-- -- -- SELECT first_name || '   ' || last_name AS nombre_completo FROM employees WHERE id = 9;

-- -- -- SELECT * FROM employees WHERE first_name LIKE 'P%';
-- -- -- SELECT * FROM employees WHERE first_name LIKE '%a%';

-- -- -- SELECT COUNT(id) FROM employees;
-- -- -- SELECT MAX(salary) FROM employees;


-- -- -- SELECT AVG(salary) FROM employees;
-- -- -- SELECT MAX(salary) FROM employees;

-- -- -- SELECT first_name FROM employees; 
-- -- -- SELECT salary FROM employees;
-- -- -- SELECT ROUND((salary), 2) FROM employees;

-- -- -- --innerJOIN

-- -- -- SELECT *
-- -- -- FROM users
-- -- -- INNER JOIN posts
-- -- -- ON users.id = posts.user_id;

-- -- -- INSERT INTO comments(post_id, user_id, body) VALUES 
-- -- -- (1, 3, 'This is comment one'),
-- -- -- (2, 1, 'This is comment two'),
-- -- -- (5, 3, 'This is comment three'),
-- -- -- (2, 2, 'This is comment four'),
-- -- -- (1, 2, 'This is comment five');


-- -- -- ------

-- -- -- CREATE TABLE departments (
-- -- --   id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY NOT NULL,
-- -- --   name VARCHAR(100) NOT NULL
-- -- -- );

-- -- -- ALTER TABLE employees ADD COLUMN department_id BIGINT REFERENCES departments(id);

-- -- -- INSERT INTO departments (name) VALUES
-- -- -- ('Human Resources'),
-- -- -- ('Engineering'),
-- -- -- ('Marketing');


-- -- -- UPDATE employees SET department_id = 1 WHERE id IN (7, 8);
-- -- -- select * from departments;

-- -- -- UPDATE employees SET department_id = 3 WHERE id = 5;
-- -- -- UPDATE employees SET department_id = 2 WHERE id = 21;
-- -- -- UPDATE employees SET department_id = 2 WHERE id = 17;
-- -- -- UPDATE employees SET department_id = 2 WHERE id = 1;

-- -- -- SELECT * FROM employees INNER JOIN departments ON employees.department_id = departments.id;

-- -- -- --Repaso SQL clase 4

-- -- -- INSERT INTO departments (name) VALUES
-- -- -- ('Finance');

-- -- -- select * from departments;

-- -- -- SELECT * FROM employees;

-- -- -- SELECT
-- -- -- employees.first_name,
-- -- -- employees.last_name
-- -- -- FROM employees
-- -- -- INNER JOIN departments ON employees.departments_id = departments.id;


-- -- -- UPDATE employees SET department_id = 3 WHERE id IN (5);

-- SELECT * FROM employees;
-- -- SELECT employees.first_name, departments.name 
-- -- FROM employees
-- -- INNER JOIN departments ON employees.department_id = departments.id;

SELECT * FROM employees INNER JOIN departments ON employees.department_id = departments.id;